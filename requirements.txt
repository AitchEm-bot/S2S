# Core dependencies
flask
requests

# Audio processing
whisper

# RAG implementation
chromadb
sentence-transformers

# Dependencies we removed:
# sounddevice (not used)
# soundfile (not used)
# numpy (not used directly, but is a dependency of other packages)
# torch (not used directly, but is a dependency of sentence-transformers)
# python-dotenv (not used) 